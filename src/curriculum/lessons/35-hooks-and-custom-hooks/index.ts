import Concept from './concept.mdx'

import { createPlaceholderLesson } from '../../placeholder'

export const lesson = createPlaceholderLesson({
  slug: 'hooks-and-custom-hooks',
  title: 'Hooks and Custom Hooks',
  summary: 'Encapsulate reusable stateful behavior while preserving React rules and dependencies.',
  track: 'frontend',
  order: 35,
  concept: Concept,
})
