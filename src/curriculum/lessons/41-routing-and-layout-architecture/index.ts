import Concept from './concept.mdx'

import { createPlaceholderLesson } from '../../placeholder'

export const lesson = createPlaceholderLesson({
  slug: 'routing-and-layout-architecture',
  title: 'Routing and Layout Architecture',
  summary: 'Structure nested routes, layouts, and navigation for durable client apps.',
  track: 'frontend',
  order: 41,
  concept: Concept,
})
