import Concept from './concept.mdx'

import { createPlaceholderLesson } from '../../placeholder'

export const lesson = createPlaceholderLesson({
  slug: 'trees-and-recursion',
  title: 'Trees and Recursion',
  summary: 'Use recursive structure to traverse, aggregate, and transform trees.',
  track: 'algorithms',
  order: 10,
  concept: Concept,
})
