import Concept from './concept.mdx'

import { createPlaceholderLesson } from '../../placeholder'

export const lesson = createPlaceholderLesson({
  slug: 'http-apis-and-request-lifecycle',
  title: 'HTTP APIs and Request Lifecycle',
  summary: 'Trace requests through routing, validation, authorization, handlers, and responses.',
  track: 'backend-data',
  order: 46,
  concept: Concept,
})
